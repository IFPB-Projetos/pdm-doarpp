import { StyleSheet } from "react-native";

export const userStyles = StyleSheet.create({
  topButton: {
    padding: 10,
    fontSize: 16,
    borderWidth: 1,
    borderRadius: 10,
    borderColor: "gray",
  },
});
